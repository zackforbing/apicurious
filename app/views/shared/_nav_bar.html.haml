%nav.navbar.navbar-default{role: "navigation"}
  .navbar-header
    %button.navbar-toggle{"data-target": "#bs-example-navbar-collapse-1", "data-toggle": "collapse", type: "button"}
      %span.sr-only Toggle navigation
      %span.icon-bar
      %span.icon-bar
      %span.icon-bar
    %a.navbar-brand{href: "/"} Zack's ApiCurious
  .collapse.navbar-collapse#bs-example-navbar-collapse-1
    %ul.nav.navbar-nav
      - if current_user
        %li
          = link_to 'Account', user_path(current_user)
        %li
          = link_to 'Activity', "#" #make this a dropdown to use with followed_user_activity and my_activity
    %ul.nav.navbar-nav.navbar-right
      - if current_user
        %li
          = link_to 'Logout of GitHub', github_logout_path
      - else
        %li
          = link_to 'Login with GitHub', github_login_path
